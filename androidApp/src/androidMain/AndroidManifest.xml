<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- For file picking (resume upload) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- For camera access (profile picture) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Declare support for different screen sizes -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:name=".VwaTekApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VwaTekApply"
        android:hardwareAccelerated="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.VwaTekApply">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep link handling for OAuth callbacks -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="vwatek-apply.web.app"
                    android:pathPrefix="/auth/callback" />
            </intent-filter>
            
            <!-- Custom scheme for OAuth -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="vwatekapply"
                    android:host="auth" />
            </intent-filter>
            
            <!-- LinkedIn OAuth callback deep link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="vwatekapply"
                    android:host="linkedin-callback" />
            </intent-filter>
            
            <!-- Deep links for in-app screen navigation -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vwatekapply" android:host="home" />
                <data android:scheme="vwatekapply" android:host="resume" />
                <data android:scheme="vwatekapply" android:host="optimizer" />
                <data android:scheme="vwatekapply" android:host="coverletter" />
                <data android:scheme="vwatekapply" android:host="interview" />
                <data android:scheme="vwatekapply" android:host="noc" />
                <data android:scheme="vwatekapply" android:host="jobbank" />
                <data android:scheme="vwatekapply" android:host="tracker" />
                <data android:scheme="vwatekapply" android:host="salary" />
                <data android:scheme="vwatekapply" android:host="linkedin" />
                <data android:scheme="vwatekapply" android:host="organization" />
                <data android:scheme="vwatekapply" android:host="subscription" />
                <data android:scheme="vwatekapply" android:host="profile" />
                <data android:scheme="vwatekapply" android:host="settings" />
            </intent-filter>
            
            <!-- Deep link for Stripe checkout callbacks -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vwatekapply" android:host="checkout" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing PDF exports -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
