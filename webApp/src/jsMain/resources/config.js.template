/**
 * VwaTek Apply Configuration
 * 
 * SECURITY NOTE: AI API keys (Gemini, OpenAI) should NEVER be exposed to the browser.
 * All AI operations are routed through the backend at /api/v1/ai/* endpoints.
 * The backend holds the API keys securely as environment variables.
 * 
 * This file is no longer needed for API key configuration.
 * If you need to configure the backend URL for development, use:
 */
window.VWATEK_CONFIG = {
    // Backend API URL (only needed for non-standard deployments)
    // API_BASE_URL: 'http://localhost:8090'
};
