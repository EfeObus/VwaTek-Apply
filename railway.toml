[build]
buildCommand = "./gradlew :backend:shadowJar :webApp:jsBrowserDistribution -x check -x test"

[deploy]
startCommand = "java $JAVA_OPTS -jar backend/build/libs/backend-all.jar"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
